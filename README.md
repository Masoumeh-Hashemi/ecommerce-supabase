## 🚀 Getting Started

After cloning the repository, follow these steps to run the app:

## Install dependencies

```bash
npm install
```
## place supabase credentials in .env file
I attached the credentials in the Email

```bash
SUPABASE_URL
SUPABASE_KEY
```

## Start the app

```bash
npm run dev
```

## Postman Collection

You can import the API collection into Postman using [this file](postman_collection.json).

To use it:
1. Open Postman
2. Click **Import**
3. Choose the `postman_collection.json` file